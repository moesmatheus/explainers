# Preferences

## Process rules (top of file — read first)
- [2026-04-25] **NEVER do an explainer build in one go.** User-stated rule, direct feedback: writing a 2,500+ line file in a single Write call sometimes crashes Claude (token limits, context drift, parser ambiguity in long JSX). The mandated workflow is the **stage-by-stage build**: (Stage A) scaffolding only — primitives + Hero + SectionNav + GLOSS + stub default export; verify it loads. (Stage B) register in `src/explainers.js` + verify scaffolding renders. (Stages C-I) add 1-2 cards per stage, run `npx vite build` after each Edit batch, eval the page after each stage to confirm card IDs render and KaTeX has zero errors. (Stage J) full QA detector sweep. (Stage K) update PREFERENCES.md. This applies to ALL future explainer builds regardless of perceived simplicity. Don't try to be clever and batch — the cost of one crashed turn is much higher than the cost of 10 small turns. _user feedback after the C build_

## Liked (keep doing)
- [2026-04-25] **Project-mapping boundary card** for explainers anchored to a specific user repo (here: `forecasters-craft` → `betting-vibe-04`). One row per content card, three columns: topic / current-state-in-the-repo / next-action. Plus a small "good / partial / gap" status chip per row, with counts as three Stat tiles at the top. Hover any row → FloatingTip with the live state description. Each card name in the leftmost column is a CrossLink back to the source card. The card becomes the operational deliverable — the reader uses it to decide what to ship next. Pairs naturally with the trilogy-callback pattern; differs in that the rows map to **the user's own files**, not to sibling explainers. _forecasters-craft/Boundary_
- [2026-04-25] **Two-spine cards explicitly named at the top** — when an explainer rests on two load-bearing principles (here: Loss Alignment + Information Hypothesis), mark BOTH with `anchor` so they get the fuchsia ring + "spine" badge in the side-nav, and have every later card include a one-line CrossLink callback to whichever spine it's a play on. Builds the through-line discipline of the trilogy without requiring a literal trilogy. _forecasters-craft/{Loss, Info}_
- [2026-04-25] **Tabbed leaderboard with re-sort + bar refill** for "the best-by-X is not the best-by-Y" arguments. Single table of N items, M metric tabs above; clicking a tab re-sorts the rows and re-fills the bars to that metric's scale (with direction-awareness for lower-is-better metrics). The fuchsia "best" highlight moves visibly between rows as the tab changes — the reader feels the metric mismatch instead of just reading about it. Worked for showing 4 toy models reshuffling under log-loss / Brier / CLV / log-growth. _forecasters-craft/Loss_
- [2026-04-25] **Slider → derived-metric grid + comparison bars** for "your output vs a reference" interactives. Pattern: 2-3 sliders for the model output (e.g. P(home/draw/away)); a 2-column grid of derived stats (KL, expected log-growth); paired bar chart comparing model vs market side-by-side per outcome. Works because the reader can move one knob and watch a coherent chain of consequences light up. _forecasters-craft/Info_
- [2026-04-25] **Algorithm-comparison curves with a budget marker** — for "method A vs B vs C convergence" cards: 3-4 monotone-decreasing curves on the same axes (e.g. random / TPE / Hyperband / BOHB at varying trial counts), plus a draggable vertical "current budget" line; below the chart, a 4-tile Stat row showing each method's value at the current budget. Re-uses the chart as both "compare across methods" and "what does my chosen budget actually buy me". _forecasters-craft/HPO_
- [2026-04-25] **Confounder-toggle DAG + spurious-correlation readout** for the causal-lens card. Three checkboxes (each a confounder), a small DAG that highlights the open back-doors when unchecked, and a 3-stat readout (true β / bias / observed β). Toggling a confounder OFF makes its arrows light up rose AND adds to the bias term — the user feels what "controlling for X" buys. _forecasters-craft/Causal_
- [2026-04-25] **Stability-selection survival grid** — for feature-selection cards, render N features × K folds as a binary heat-grid (3px squares per fold), with a draggable π threshold bar that highlights surviving features in emerald. Counts the survivors live. Makes "select features that survive across folds" tactile in a way that a method-name-only table can't. _forecasters-craft/Selection_
- [2026-04-25] **Pathway-map visual for adaptive plans** — render DAPP-style pathways as horizontal bars on a SLR-axis, each bar's start/end indicating its validity range, with a draggable "now" marker. As the user slides the marker, the pathways currently active (where now ∈ [start, end]) light up; tipping points are visible as right-edge markers. Reader builds an intuition that the plan is the tree, not any single bar. _deep-uncertainty/DAPP_
- [2026-04-25] **2×2 scenario matrix as the load-bearing visualization** — for any deep-uncertainty topic, the central artifact is the 2×2 quadrant of "two deeply-uncertain critical drivers crossed". Render as a clickable SVG: 4 named quadrants, each showing scenario name + headline number + label. Click → expand details + signposts + actions. The reveal flow (overview → click → reveal actions) makes the structure of scenario planning operational, not just descriptive. _deep-uncertainty/Scenarios_
- [2026-04-25] **Regret matrix as the synthesis check** — at the anchor, render a strategy × scenario matrix of regret values (in $ or any cost unit). Color cells green/amber/rose by regret magnitude. Right-most column is "worst-case regret across scenarios" — the headline metric. The robust portfolio's worst-case regret should be smallest. Pattern: makes the case for robustness over optimality visible at a glance, no math required. _deep-uncertainty/Anchor_
- [2026-04-25] **Five-criteria comparison table for decision-making** — for the "decision under deep uncertainty" card, lay out maximin / maximax / Laplace / minimax-regret / Hurwicz as a tab selector over the same 3-row × 3-col payoff matrix. Cells re-color when criterion changes; one row gets ★. Adding the Hurwicz α slider when "hurwicz" is active is the right disclosure pattern — only show the parameter that's relevant to the active mode. _deep-uncertainty/Criteria_
- [2026-04-25] **Operational responses palette · 6 hover-cards** — for the "what do I actually DO" card, render 6 named responses (hedging / redundancy / optionality / antifragility / precaution / slack) as a 2-col grid of hover-cards. Each shows what / examples / cost / benefit / when. Single floating tip aggregates more detail. Pattern lives between the framework cards (abstract) and the anchor (synthesis); makes the operational layer concrete. _deep-uncertainty/Operational_
- [2026-04-25] **Two anchor cards in one explainer** — when the topic has both a *technique-anchor* (the load-bearing concept) and a *case-anchor* (the worked example), star both. In statistical-forecasting we starred ★ Probabilistic outputs (the load-bearing technique that everything else feeds into) AND ★ Anchor: ERCOT week (the synthesis). Reader sees them flagged in the side nav and gets two natural "open here first" landing points. Each carries a `anchor` Card prop that gives a fuchsia ring on the section. _statistical-forecasting/{Probabilistic, Anchor}_
- [2026-04-25] **Synthetic but quantitatively realistic series for visualization** — the explainer needs to run *in the browser* without backend calls, so generate a deterministic seeded synthetic series at module scope (`buildDailySeries({...})` returns 10y daily, `buildHourlyWeek({...})` returns 168h). Tune amplitudes/seasonality so the visual matches what real ERCOT data would look like (~38 GW base, summer peaks, weekend dips, secular growth, an Uri-shaped shock at year 6 day 44). Compute decompositions/forecasts on import; cards render instantly. Pattern: scaffolding stage exports the series, every card imports it. _statistical-forecasting/scaffolding_
- [2026-04-25] **Live-fitted models in JS for ETS / Prophet / GBM-importance / decomposition** — when the math is small and parametric, write the actual fit in JS (Holt-Winters update equations, ACF/PACF via Durbin-Levinson, ridge-regressed Fourier seasonality, naive piecewise-linear+Laplace-shrunk trend). The user drags α/β/γ/φ/τ/K and the chart updates in real time. Doesn't replace a real implementation but makes the math feel <em>operational</em> instead of declarative. ~80 lines per fit, runs in <1 ms. _statistical-forecasting/{ETS, Prophet, ARIMA-ACF}_
- [2026-04-25] **Multi-band prediction interval render** — overlapping translucent SVG polygons for 50% / 80% / 95% intervals, with central forecast as a solid line and realized values as a dashed continuation past the train/forecast boundary. Three-tier opacity (0.30 / 0.18 / 0.10) reads cleanly even on ~5° narrower viewport. Pattern reused on the probabilistic anchor and the synthesis anchor. _statistical-forecasting/{Probabilistic, Anchor}_
- [2026-04-25] **Coverage-on-holdout table with auto-color** — for any probabilistic forecast, print a 3-cell row of stats (50%/80%/95%) showing observed coverage with `over by X pp` / `under by X pp` sub-text and color-coded green/amber/rose based on |gap| < 5pp. Calibration becomes visceral; the green pp tells the reader the model is trustworthy without needing to read the full reliability theory. _statistical-forecasting/Probabilistic_
- [2026-04-25] **Side-by-side method comparison via tab buttons** — for "compare 3-4 methods on the same chart" patterns: place compact tab buttons just above the chart, each highlighted with its method's accent color when active, chart re-renders instantly. Used for: ACF/PACF mode (AR/MA/seasonal/diff), CV strategies (rolling/expanding/leaky), reconciliation methods (base/BU/TD/MinT). Lower-friction than separate cards; reader can A/B in their head. _statistical-forecasting/{ARIMA, CV, Hierarchy}_
- [2026-04-25] **Coherence indicators inline** — for the reconciliation card, render `✓ sum 19.5` (emerald) or `✗ sum 19.5 vs 22.5` (rose) directly under each parent node in the SVG hierarchy. The reader sees in one frame whether children sum to parents — coherence is the whole point of reconciliation, and showing the violation visually is more memorable than a paragraph. _statistical-forecasting/Hierarchy_
- [2026-04-25] **Method-comparison bar chart with naive baseline tick** — vertical line at the naive-baseline value (MASE=1) anchors every other bar. Each bar colored by method, sorted descending. Comparison is "how far below the naive line am I". Pattern: emerald/cyan/amber/violet for stat/ML/Prophet/Naive families. _statistical-forecasting/Ensembles_
- [2026-04-25] **End-to-end synthesis card on the anchor**, after building blocks have been introduced — anchor card states the question formally (event/deadline/arbiter/defaults), then walks the reader through *every* prior technique applied to it: outside-view geometric mean of reference classes → Fermi multiplicative chain → Bayesian log-odds update with named evidence → 50/50 voice average → mild extremize. Final number rendered as a 4xl-mono "headline forecast" inside a fuchsia callout, plus a "calibration graveyard" of 4 common wrong intuitions with one-liner explanations of why each is wrong. The card carries the whole explainer — the reader can leave knowing the chain even if they skipped earlier cards. _superforecasting/Anchor_
- [2026-04-25] **Predict-then-reveal at the anchor**: a probability slider locked at the user's guess and a "reveal synthesis" button — the user must commit a number (slider defaults to 30%) before they can see the model's chain. Active recall + makes the chain feel diagnostic instead of declarative. _superforecasting/Anchor_
- [2026-04-25] **"Walk the ladder" layered rewrite** for showing how to operationalize a fuzzy question — same question rewritten at 4 escalating levels (vague → concrete event → resolution criteria → arbiter+defaults), with a chip toggle between levels and a per-level "remaining ambiguity" list. Final level gets an emerald "resolvable, edge cases handled" check. Forces the reader to *see* what each escalation buys. _superforecasting/Operationalize_
- [2026-04-25] **Reliability diagram with bin-size-encoded dot radius** — points sized by sqrt(n_in_bin), connector lines from diagonal to point showing miscal direction, hover gives `(p̄, ō, n, miscal²)`. Below the chart, 5 stat tiles for B / REL / RES / UNC / Skill with green-amber-red text per band. Profile picker (super / pundit / hedger) cycles three pre-baked tracks instantly so the reader sees the *shape* differences, not just the headline number. _superforecasting/Calibration_
- [2026-04-25] **Reference-class picker with implied-prior gauge** — 4 candidate classes shown as buttons with `yes/n` and `%` columns; clicking one expands the description, why-it-might-be-wrong, and an animated horizontal-bar gauge for the prior. Below: a "putting the four side-by-side" worked block, leading naturally into the geometric-mean aggregation argument. _superforecasting/ReferenceClasses_
- [2026-04-25] **Multiplicative-chain Fermi calculator** with low/median/high sliders per factor (interpolated log-linearly through the 3 anchors), plus log-error propagation giving a 95% interval AND a `P(≥ target)` derived from the implied log-normal — one slider drag updates all three readouts. Shows the user that uncertainty has structure: which factor's σ_log dominates is visible as you slide. _superforecasting/Fermi_
- [2026-04-25] **Log-odds Bayes updater with togglable evidence** — single prior slider + N evidence rows, each with on/off checkbox AND its own LR slider (0.1–10, log-scaled). Posterior bar with a small ticker showing `Σ log(LR)` and "evidence net favors YES/NO". The toggle mechanic lets the user feel how much each piece moves the result individually vs in combination. _superforecasting/Bayes_
- [2026-04-25] **AOM self-check with 1–5 button rows + reverse-scoring** — 6 statements, half reverse-scored (flagged with a small chip), live aggregate avg + band (low/medium/high). Cheap, immediate, embeds the concept by making the user think about each statement instead of reading abstract definitions. _superforecasting/Mindset_
- [2026-04-25] **Premortem prompts as collapsible cards** — 3 click-to-expand prompts, each opening a list of pre-baked "surfaced reasons". Pairs with a tripwire grid below where each tripwire is tagged bull/bear with an estimated probability shift (e.g. `+15 pp` if observed). The pair operationalizes "what would change my mind?" without requiring the reader to write anything. _superforecasting/Premortem_
- [2026-04-25] **Aggregation visual on a number line** with 5 colored dots (forecasters) + a dashed "average" tick + a green star for the extremized result + a slider for exponent `a`. The user drags `a` from 1→3 and sees the star slide away from 50%; intuition becomes muscle. _superforecasting/Aggregate_
- [2026-04-25] **Calibration-decay curves on log-x time axis** with 3 lines (super / regular / always-50%-baseline) and an HTML legend chip row above the SVG (NOT inline labels at the right edge — those overflow at common viewport widths). Always render legends as HTML chips, never as `<text>` past `x=W-P`. _superforecasting/Horizons_
- [2026-04-25] **Bull-case / bear-case side-by-side lists at the anchor**, each with 5 concrete signs-to-watch — natural pair with the tripwire card and gives the reader an actionable "watchlist" deliverable. Pattern: emerald TrendingUp icon + emerald header / rose TrendingDown icon + rose header. _superforecasting/Anchor_
- [2026-04-25] **Pull-quote in a left-bordered callout** at the synthesis end — `border-l-4 border-fuchsia-400/50` with a Quote icon, italic body, attributed name in uppercase fuchsia eyebrow. Lets the explainer end on a thought, not a stat. _superforecasting/Anchor_
- [2026-04-24] **Anchor card at the center of a broad survey** — for an explainer with a specific real-world dilemma at its heart (here: "locação vs serviço" for data center colocation), mark the anchor card with ★ in the title, keep all preceding cards as <i>building blocks</i> for it, and make the anchor the densest card: MinSchema + Predict with reveal + interactive slider + two side-by-side comparison boxes + quote from legal source + Misconception + two lists of "sinais de X" / "sinais de Y" + Deeper on why-it-matters-now. Gives the whole explainer a center of gravity the user can open to immediately. _tributacao-brasil/DCLocServ_
- [2026-04-24] **Sector-comparison stacked bars "where does R$100 go"** — for any policy/regime/cost topic, a single card showing three representative archetypes (e.g. SaaS / Indústria / Data Center) as stacked horizontal bars of where each real-unit of revenue ends up in tributes. More legible than a tax-by-tax table because it forces the eye to compare <i>composition</i>, not just totals. Hover titles on each segment; full legend below. Total of tributes shown at the right of each bar as compact check. _tributacao-brasil/Bolo_
- [2026-04-24] **PT-BR pedagogy primitive labels** — "leve isso", "quando importa", "mito comum" (with "muita gente pensa" / "na verdade" / "por quê"), "tenta prever" / "revelar" / "esconder", "exemplo numérico", "auto-checagem · clique para revelar". Kept the same shapes/colors as the English set, just relocalized. The result reads completely native; the conceptual architecture (predict → reveal, confront misconception, ground the intuition) carries over unchanged. _tributacao-brasil_
- [2026-04-24] **Timeline of transition as dual horizontal bars** — for any multi-year transition that phases out A and phases in B (here: reform 2026 → 2033), render each year as two parallel bars: A shrinking, B growing, with a short text note in the right column explaining what qualitatively changes that year. Immediately legible, and carries 3 dimensions (year × new-system-alíquota × legacy-% remaining) in a single rectangle grid. _tributacao-brasil/Reforma_
- [2026-04-19] **Glossary-backed hover tooltips for hard technical terms** — user praised this after the data-centers rollout: "i really liked this practice... especially for more technical subjects". Pattern: one `GLOSS` object at the top of the file keyed by the exact string, plus a `<Term>` component that renders a dotted underline (`decoration-dotted decoration-violet-300/60 underline-offset-[3px] cursor-help`) and a `FloatingTip` tooltip showing `TERM NAME` (uppercase violet eyebrow) + 1–2 sentence definition. Reuses the existing `FloatingTip` portal primitive. Tooltip `def` prop lets you override per-instance without touching the glossary. Aim for "safe side of equilibrium" — err toward *more* definitions rather than fewer, but wrap ~first occurrence per paragraph/section so underlines don't become noise. Especially important in Deeper blocks where term density is highest. _data-centers/DataCentersExplainer.jsx — `Term` and `GLOSS`_
- [2026-04-19] **"Why + trade-offs" paragraphs in Deeper blocks** — user praised the raised-floor-vs-slab commentary in `data-centers/Anatomy`: "i love those bits where you add information of why things are done in a certain way and the trade offs". Every Deeper block should include at least one paragraph that explains WHY something is designed a particular way and what is being traded off against what (cost vs density, serviceability vs efficiency, lock-in vs tuning burden, etc.). Keep it concrete — specific thresholds ("under-floor plenums can't move enough air for 50+ kW racks"), specific numbers, specific named alternatives. Avoid generic "pros and cons" lists. _data-centers/Anatomy_
- [2026-04-19] **Sankey diagrams for flow-type data** — "i really like those". Any concept that is "X produces → Y consumes" or "sources → instruments → destinations" should default to a Sankey over bars. Offer a tab selector when the flow has a categorical dimension (commodity, period, etc.). _world-economy/Consumption, CashFlows_
- [2026-04-19] **"Size map" pattern for ranking data** — when user asks "which are the biggest / most relevant", build a long ranked-bar view (sorted desc, rank number + label + bar + value + growth% columns) with a unit-switch tab (e.g. Industries $T vs Commodities $B). Include inline scale ticks and hover with top-3 producers + growth + unit + one-liner note. _world-economy/SizeMap_
- [2026-04-19] **Squarified treemap for "by group" hierarchical size** — when a dataset has a natural 2-level grouping (e.g. 5 classical sectors → N industries inside each), add a treemap tab alongside the ranked bars. One color per group; group header label in bold uppercase on a darker semi-opaque backdrop inside each region; tile labels (name + $ value) drawn only when tile is big enough (`w>52 & h>22`). Hover tile shows the group chip, name, value, a mini-bar breakdown of top producer shares, and a one-liner note. Layout via the squarify algorithm (Bruls/Huijing/van Wijk 2000) applied twice — once for groups, once per group for items. _world-economy/SizeMap_
- [2026-04-19] **Rich hover tooltips on every chart element** — "for charts overall i like hovering behaviour to show information or even additional stuff". Minimum: full label + primary value + one derived stat (share of total, % imbalance, net position). For multi-part composites (stacked bars, scatter), show the full breakdown with the hovered segment emphasized. _world-economy_
- [2026-04-19] **Splitting chord/corridor arcs into two directional arcs** with a perpendicular offset, sized by one-way flow each. Makes imbalances immediately legible ("where the two arcs are visibly unequal, the corridor is running a surplus") — "amazing". _world-economy/Trade_
- [2026-04-19] **Scale-reference key** inline in the chart (tiny benchmark marks at $100B / $300B / $600B etc.) whenever a chart encodes a quantity into thickness/area/radius. Reader should never have to guess what a "thick" line is worth. _world-economy/Trade_
- [2026-04-18] Manim "cinema-cut" video embedded *next to* (not replacing) the interactive React visual, framed in a captioned player — pattern confirmed for reuse in future explainers when a concept benefits from true time-domain animation. _quantum-mechanics/Tunneling_
- [2026-04-17] High-contrast dark palette (sky/violet/fuchsia on neutral-950) — "contrast looks really good". _quantum-mechanics_
- [2026-04-17] Double-slit dot-accumulation visual with a toggleable which-path detector — "brilliant". _quantum-mechanics_
- [2026-04-17] Animated Re/Im Schrödinger wave with the equation typeset above it — "brilliant". _quantum-mechanics_
- [2026-04-17] Heisenberg side-by-side Gaussians with a single slider driving the trade-off — "brilliant". _quantum-mechanics_

## Disliked (avoid)
- [2026-04-19] Two colors from the same hue band in one legend (e.g. Tailwind `yellow-300` + `amber-400` for Cash vs Gold). On a dark background they read as the same color. When a legend has >4 entries, force the palette to span distinct hues; save yellow for the single most "gold-like" thing. _world-economy/Wealth_
- [2026-04-19] Labels / headers that wrap and collide with their subtitle. Any two-part header (e.g. "sovereign wealth funds · AUM" + "total ≈ $8.8T") must use `flex flex-wrap items-baseline justify-between gap-x-3 gap-y-1`, not a single-line flex with `justify-between`. _world-economy_
- [2026-04-19] Labels that extend past the chart's viewBox / right edge (e.g. country names on a line chart right-edge). Always budget enough right-padding in the viewBox for the longest label. _world-economy/Drivers_
- [2026-04-17] Animated visuals that auto-run too fast with no speed control — user wanted to slow down the tunneling animation. _quantum-mechanics_
- [2026-04-17] Inline subscripts/superscripts rendered as plain underscores/carets (e.g. `E_n`, `e^(−2κL)`) — must typeset as real `<sub>`/`<sup>` or Unicode. _quantum-mechanics_

## What worked well
- [2026-04-25] **`npx vite build` as a safety net** — for heavy single-file explainers (2,500+ lines), running `npx vite build` after each card-pair Edit catches JSX parse errors that HMR will silently swallow. Adds ~2s per build. The detector / eval suite verifies render correctness; the build verifies parse correctness. Two layers, both cheap. _deep-uncertainty build_
- [2026-04-25] **Trilogy structure ratifies itself in the boundary card** — building 3 explainers in sequence (A → B → C) feels disjointed until the third one's "where each toolkit fails" card lands. That single card retroactively makes the series feel intentional. Defers integration work to the moment when all three are on the table; cheaper than trying to integrate from the start. _deep-uncertainty/Boundary_
- [2026-04-25] **Cross-explainer trail callouts in NextSteps** — when building a planned series of explainers (A judgmental, B statistical, C deep uncertainty), each card's NextSteps explicitly cites the others by slug. statistical-forecasting points back to `/#superforecasting` and forward to `(C, planned)` as a non-link "find elsewhere" item. Builds the mental model that these are sibling branches of one tree, not isolated explainers. _statistical-forecasting/NextTrails_
- [2026-04-25] **Series-as-deliverable framing** — when the user said "execute B" referring back to an earlier brainstorm, we committed immediately to slug, anchor question, and 13-card lineup without re-asking each piece. Treat the prior brainstorm as locked unless the user flags otherwise. Saves 3-5 question rounds; the user can always course-correct mid-build. _superforecasting → statistical-forecasting handoff_
- [2026-04-25] **Stage-by-stage scaffolding when the file will be huge** — for an explainer expected to land at 2,500+ lines, write the scaffolding (imports, primitives, Hero, SectionNav, stub default export) first; verify it loads with H1 + nav rendering; then add cards 2 at a time, running KaTeX-error + section-id eval after each pair. Catches typos and ID drift incrementally — much better than a single Write that fails compilation and forces re-reading 2k lines to find the bad line. _superforecasting build_
- [2026-04-25] **DOM eval suite as primary QA verification** for very tall pages where `whileInView` and screenshot capture are unreliable. The 8 detector eval queries (KaTeX errors, SVG overflow, SVG overlap, contrast, numeric format, truncation, dup defs, dead clickables) collectively give higher confidence than a visual review and don't require any animation to settle. Screenshots become optional — used only when a layout question can't be answered by the DOM. _superforecasting QA run_
- [2026-04-19] **Single shared `FloatingTip` primitive** for every chart hover — mouse-tracked via `clientX/clientY`, portaled to `document.body`, auto-flips near the right/bottom edge, renders any JSX the chart passes in. Rewrote the whole explainer's hover UX in one component. _world-economy_
- [2026-04-19] **Data atlas rhythm for a broad quantitative topic**: 8 cards, each opening with an intro paragraph, a single hero chart with hover, a takeaway paragraph, then a "Deeper" collapsible block. Worked for "The World Economy"; likely reusable for any world-scale topic (geopolitics, climate, energy) where the point is to show real numbers, not derive formulas.
- [2026-04-17] Each concept as its own card with equation → interactive visual → short intuition note is the right rhythm.
- [2026-04-17] Animated morph between wave and particle driven by a single slider (wave-packet envelope) communicated duality well.
- [2026-04-17] KaTeX with semantic color macros (`\op` violet operators, `\ket` sky kets, `\co` pink amplitudes, `\const` amber constants) — much easier to read than monochrome math.

## Gotchas (don't repeat)
- [2026-04-25] **Backslash-quote escaping inside JSX attribute strings BREAKS the parser, even though backslash-apostrophe is "tolerated" elsewhere.** Writing `because="Taleb\'s point is that the assumption \\\"finite variance\\\" fails"` triggers `Expected ">" but found ","` from esbuild — the doubled `\\\"` ends the attribute prematurely. Two safe forms: (i) just write the apostrophe directly (`Taleb's point...`); (ii) drop the inner quote and use em-dash phrasing (`the assumption — finite variance — fails`); (iii) use a curly-brace child: `because={`Taleb's point...`}`. Don't escape with `\\\"`; never works. The build error is loud (HMR reports failed reload) but eval at the same moment can still return previous-render results, masking the issue — always run `npx vite build` after a heavy edit to catch parse errors immediately. _deep-uncertainty/FatTails — first failed reload in this codebase_
- [2026-04-25] **Edit success ≠ HMR success.** When Edit returns "successfully", the file IS updated, but Vite may fail to hot-reload it due to a syntax error introduced by the edit. Running `eval` against the page returns whatever the previously-loaded module is rendering. Symptom: cards array is `[]` despite the new code being on disk. Diagnostic: check `[vite] Failed to reload` in console logs OR run `npx vite build` to surface the parse error directly. Now part of the heavy-explainer workflow: build-check after each Stage. _deep-uncertainty build_
- [2026-04-25] **Section-nav `<a href="#section-id">` was kicking the hash router back to Home on every in-page anchor click.** Root cause: `App.jsx` does `explainers.find(e => e.slug === hash)` and falls back to `<Home/>` if no match — so clicking a side-nav link with `href="#calibration"` set the hash to `calibration`, no slug matched, and the user landed on Home. Affected all 11 explainers since they share the same hash-router. Single-file fix: in `App.jsx`, track `lastSlugRef.current` of the last-mounted explainer, and when `hash !== ''` and matches no slug, fall back to that ref instead of Home. Preserves URL-as-deep-link for slugs (`#superforecasting` works) AND in-page scroll-to-section (browser scrolls to `#calibration`, router stays on the explainer). _src/App.jsx_
- [2026-04-25] **`motion.circle` / `motion.text` animating SVG attributes (cx, x, etc.) warns "from undefined to N" even with explicit `initial`** in framer-motion v11. The fix that actually silenced it: drop motion entirely on the SVG element and use a plain `<circle cx={...} style={{ transition: 'cx 0.25s ease' }} />`. The CSS transition does the same job and React just diffs the attribute. Don't fight motion on bare SVG attributes — use motion for `style`/`transform`/CSS-numeric props, plain SVG with CSS transitions for `cx`/`x`/`r`/etc. _superforecasting/Aggregate_
- [2026-04-25] **End-of-line legend labels in a chart's SVG always overflow the viewBox at narrow viewports.** Pattern: `<text x={sx(maxX)+5}>label</text>` puts text past the right edge whenever `sx(maxX) + label_width > W-P`. Caught by the layout detector — superforecaster / regular forecaster / always-50% labels overflowed by 33–62 px. The reliable fix: lift the legend out of the SVG and render it above the chart as an HTML chip row (`<span class="inline-flex"><span class="w-3 h-0.5 bg-emerald-300" />superforecaster</span>`). Cheaper, narrower-viewport-safe, more consistent typography. _superforecasting/Horizons_
- [2026-04-25] **`whileInView` opacity transitions stall at intermediate values on very tall pages (~24k px, 12+ heavy cards).** Even the documented "scroll-to-top → wait → scroll-to-target → wait 1.5s" sequence sometimes lands a card at opacity ~0.05 instead of 1, because IntersectionObserver fires but the transition doesn't complete before the screenshot. For QA, prefer DOM-level eval verification over screenshot inspection. If a screenshot is required, run `document.querySelectorAll('section[id]').forEach(el => { el.style.opacity='1'; el.style.transform='none'; })` AFTER scroll, then screenshot — but note this still occasionally fails on heavy pages. _superforecasting QA run_
- [2026-04-25] **Tailwind dynamic class strings via `chipPalette[color].split(' ')[N]`** — using array-indexed splits of a literal class string DOES work because the literal is in the source, but is fragile: any palette refactor that reorders the words breaks every consumer. Better: structure the palette as an object `{ bg, text, border }` from the start, and pass `chipPalette[color].border` etc. Less clever, much easier to refactor. _superforecasting/Operationalize, ReferenceClasses_
- [2026-04-25] **Edit tool can return "successfully" when the edit silently didn't apply** — likely a whitespace/escape mismatch in `old_string`. Always verify with grep or fresh Read after a meaningful edit, especially on files you haven't touched recently. Don't trust the success response alone. _superforecasting PREFERENCES update_
- [2026-04-25] **In-SVG axis titles fight digit labels at the viewBox edge.** Putting `<text>` axis titles ("home goals →", "← away goals", "CV ½-width") inside an SVG near its borders almost always overlaps with the tick-digit labels or with the SVG bounding box itself. The detector flagged 3 overlaps + 1 left-overflow on the first pass. Fix: lift the axis titles OUT of the SVG entirely. Use a CSS grid wrapping the SVG with two cells: a vertical-orientation div on the side (`writingMode: vertical-rl; transform: rotate(180deg)` for a "↑ away goals" caption) plus a centered HTML caption below for the x-axis. Cleaner typography, no overlap, narrower-viewport-safe. _forecasters-craft/Distributional, Synth_
- [2026-04-25] **Side-nav `label` strings with `&` — pick one strategy and apply it consistently in both the desktop and mobile nav blocks.** Two forms work: (a) plain string `"Drift & online"` rendered as `{s.label}` (since the string literal contains a real `&`, React renders it correctly); (b) entity-encoded string `"Drift &amp; online"` rendered via `dangerouslySetInnerHTML={{ __html: s.label }}`. Don't mix — if one nav uses (a) and the other uses (b) you get visible literal `&amp;` text on one and the real ampersand on the other. _forecasters-craft/SectionNav_
- [2026-04-24] **JSX parses bare `<` as an element opener, even inside text nodes.** Writing `SaaS B2B < R$ 78M opta por Presumido` inside a `<p>` breaks compilation with "Identifier directly after number" pointing at the `<`. Fixes: use `&lt;`, replace with natural-language phrasing ("até R$ 78M", "menor que"), or wrap the comparison in `{}` as a string literal. This is a common Portuguese-language pitfall because "X menor que Y" translates mentally to `X < Y` and your fingers follow. _tributacao-brasil/PisCofinsCard_
- [2026-04-24] **Tailwind JIT needs literal class names, not template strings.** Writing `` className={`bg-${color}-500/20`} `` never compiles `bg-emerald-500/20` into the final CSS because Tailwind scans source for literals. Two workarounds that work: (i) pre-resolved lookup object `const palette = { emerald: { bg: 'bg-emerald-500/20', ... } }` passed to children; (ii) let the parent pass the full class string as a prop (`bar="bg-emerald-500/70"`). Trying to be clever with `.replace('text-', 'bg-')` at runtime on a literal class also fails — Tailwind still needs to see the literal at <i>build</i> time. _tributacao-brasil/MapaFederativo, Panorama_
- [2026-04-24] **framer-motion's `whileInView` makes programmatic scrolling + instant screenshot useless.** Cards sit at opacity 0 until the IntersectionObserver fires. When you `scrollIntoView` in a preview-eval IIFE, screenshot immediately after, you capture a black screen because the transition hasn't triggered. Fix: scroll to top first, wait ~400ms, scroll to target, wait ~1.5s — that gives the observer two state changes to latch. Otherwise the viewport's occupancy never changes and the card stays invisible. _tributacao-brasil QA run_
- [2026-04-24] **Preview MCP viewport defaults tiny and resets on reload.** The default preview size is often 240×716 until `preview_resize` is called, and sometimes reload reverts it. Always resize up front (desktop or explicit 1280×800) and re-resize after any navigation/reload. Otherwise QA detectors report hundreds of false-positive overflows that are just viewport narrowness. _tributacao-brasil QA run_
- [2026-04-19] **`position: fixed` tooltips get rebased by `transform` on an ancestor.** framer-motion `<motion.div>` wrappers (used inside `Card`) create a new containing block, so a naive `fixed` floating tooltip renders hundreds of pixels off-cursor and can clip on the right side of a card. Fix: portal the tooltip to `document.body` via `createPortal`. Also: measure the tooltip's actual `offsetWidth` in `useEffect` and flip it to the left of the cursor when `x + gap + width > vw - margin`. Pattern lives in the `FloatingTip` primitive in `world-economy/WorldEconomyExplainer.jsx`. _world-economy_
- [2026-04-17] **KaTeX macros and `#`**: inside a macro body, `#` starts a parameter ref (`#1`). Hex colors like `#c4b5fd` MUST be doubled to `##c4b5fd`, otherwise KaTeX throws "Not a valid argument number" and the equation falls back to raw red TeX. _quantum-mechanics_
- [2026-04-18] **Manim lives under `<repo>/.tools/manim/`** (shared across explainers). `vite.config.js` and `src/index.css` are configured to exclude `.tools/**` so its render cache doesn't crash Tailwind's CSS analysis. Details: auto-memory `tool_manim.md`. Default to React/SVG; reserve Manim for "cinema-cut" cases that genuinely need true time-domain animation.

## Open questions / to try
- Default posture: go deeper and more technical — include more math/formalism, derivations, and follow-up equations, not just the headline one.
- Add a "Deeper" block at the bottom of each card for extra formulas and technical commentary (keeps the headline clean).
- Every explainer should have a visible section-nav pane that tracks scroll position.
- For animated sections (tunneling, Schrödinger, spin), always include a speed/pause control.
- Polish the double-slit further: make labels and regions pop more without cluttering.
